# Hermes STS TODO

## 本次语音体验目标的后续事项

- [ ] 实机连续使用 10-20 轮，记录首声延迟、总延迟、取消/打断是否自然，以及是否仍出现异常长音频。
- [ ] 观察 LLM 流式首句播报在真实工具调用场景下的表现；如果工具调用经常在首句后才出现，需要改为更保守的短缓冲策略。
- [ ] 为真实 OpenAI-compatible SSE 工具调用补一组集成级样例，覆盖不同服务的 `tool_calls` 流式 delta 格式。
- [ ] 给总览页补一次人工视觉验收：桌面宽屏、窄屏、移动宽度下确认链路状态、指标卡、上下文操作不挤压或遮挡。
- [ ] 对最近异常/慢点做更细的事件来源：区分 LLM 超时、TTS 合成慢、音频过长、tool/web search 失败。
- [ ] 对 qwentts.cpp 参数继续 A/B：`--max-new`、`--codec-chunk-dur`、`--codec-left-dur` 在音质和首包速度之间找稳定默认值。
- [ ] 如果长文本仍偶发拖慢，把 TTS 分段策略改为“首句快发 + 后续按标点/长度自适应”，并记录每段耗时。
- [ ] 给“LLM 流式首句”开关补 UI tooltip 之外的运行指标标记，方便对比开启/关闭后的真实收益。

## 控制台信息架构后续事项

- [ ] 把“运行参数”里的底层配置继续分层，默认只露出排障常用项，低频参数折叠到高级区。
- [ ] 连接配置页的 profile 字段继续减负：保留调用路径、地址、模型、API key，其他参数默认折叠。
- [ ] Tools 面板补最近调用状态：最近调用时间、成功/失败、返回摘要、客户端是否已注入。
- [ ] Web Search 诊断补 provider chain 运行态：最近成功 provider、冷却中的 provider、最近失败原因。
- [ ] 记忆页继续弱化 URI 和底层字段，优先展示“助手记住了什么”和“最近如何影响回答”。
